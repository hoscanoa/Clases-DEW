@model IEnumerable<PREGUNTA2.Models.clientes>

@{
    ViewBag.Title = "Index";
    WebGrid wg = new WebGrid(Model);
}

<h2>Lista de Clientes</h2>
@wg.GetHtml(columns:new[]{
    wg.Column("IdCliente","Id"),
    wg.Column("DirCliente","Dirección"),
    wg.Column("fonoCliente","Teléfono")
})